# HackGeniX - Google Colab Requirements
# Optimized for Colab runtime 2025.07 (Python 3.11.13, PyTorch 2.6.0)
# 
# Usage: pip install -r requirements-colab.txt
#
# Key differences from requirements.txt:
# - No version pins (let pip resolve against Colab's pre-installed packages)
# - Excludes torch/numpy (use Colab's pre-installed versions)
# - Excludes dev/test dependencies (pytest, black, mypy, isort)

# ============= Core Backend =============
fastapi[all]
uvicorn[standard]
pydantic
pydantic-settings
python-multipart

# ============= Database =============
motor
pymongo
redis

# ============= Storage =============
boto3
python-dotenv

# ============= Document Processing =============
pypdf2
pdfplumber
python-docx
pymupdf
pytesseract

# ============= NLP & Embeddings =============
# torch - USE COLAB PRE-INSTALLED
# numpy - USE COLAB PRE-INSTALLED
sentence-transformers

# ============= LLM Inference =============
openai

# ============= Voice Processing =============
TTS
pyttsx3
faster-whisper
soundfile
pydub

# ============= Report Generation =============
reportlab

# ============= Text Analysis =============
nltk
textblob

# ============= Utilities =============
httpx
aiofiles
python-jose[cryptography]
passlib[bcrypt]
tenacity
structlog
